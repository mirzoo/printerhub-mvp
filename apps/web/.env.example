# Production: Neon Postgres connection string
DATABASE_URL=

# Production: generated automatically when a private Vercel Blob store is linked
BLOB_READ_WRITE_TOKEN=
STORAGE_DRIVER=blob

# Generate each value independently: openssl rand -base64 32
SESSION_SECRET=
REQUEST_HASH_SECRET=
CRON_SECRET=

# Shared pilot PIN, at least 6 characters. Configure only in Vercel/local env.
KIOSK_PIN=

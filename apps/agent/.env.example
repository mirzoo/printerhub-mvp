API_BASE_URL=http://localhost:3000
DEVICE_ID=printer-001
DEVICE_TOKEN=dev-device-token
PRINTER_NAME=Brother_DCP_1600_series
PRINT_MODE=dry-run
POLL_INTERVAL_MS=2000

# Optional if pdfinfo is not on PATH
# PDFINFO_PATH=/absolute/path/to/pdfinfo

# Build once with: npm run build:scanner -w @printerhub/agent
SCANNER_HELPER_PATH=native/.build/printerhub-scan
# SCANNER_NAME=Brother DCP-1600 series
KIOSK_LOOPBACK_PORT=17654
